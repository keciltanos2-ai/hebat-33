Netlify-ready package created.

Set environment variable GROQ_API_KEY in Netlify dashboard (Site settings → Build & deploy → Environment).
Optionally set GROQ_MODEL and GROQ_API_ENDPOINT.

Build: upload this repo as-is to Netlify. Functions live in netlify/functions.
